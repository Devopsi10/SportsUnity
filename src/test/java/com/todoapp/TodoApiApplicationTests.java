package com.todoapp;

import org.junit.jupiter.api.Test;
//import org.springframework.boot.test.context.SpringBootTest;

//@SpringBootTest  // Tells Spring Boot to look for the main configuration class (i.e., @SpringBootApplication)
class TodoApiApplicationTests {

    @Test
    void contextLoads() {
        // This test simply ensures that the Spring application context loads successfully.
        // If the context fails to load, the test will fail.
    }
}
